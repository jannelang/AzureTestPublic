name: Windows PowerShell
on:
    push:
      branches: [main]
      paths:
          - 'Bicep/*'
    workflow_dispatch:  
    
jobs:
    build:
      name: Run Script
      runs-on: windows-latest
      steps:
        - uses: actions/checkout@v1
        - name: Test Script
          run: ./!build/test.ps1
          shell: powershell
