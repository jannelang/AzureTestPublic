name: Powershell Test
on:
 push:
  branches: [main]
  paths:
   - 'Bicep/**.bicep'
 workflow_dispatch:
 
jobs:
 build:
  name: Snyk IaC scan
  runs-on: self-hosted
  
  steps:
   - name: Checkout
     uses: actions/checkout@v2
   - name: Compile Bicep to ARM
     working-directory: ./
     run: ./test3.ps1
     shell: pwsh
   - name: Run Snyk IaC scan
     run: ./test2.ps1
